node_modules/
.DS_Store
dist/
log/
build/
.workbuddy/
.env
.env.local
.env.*.local
*.log
.vscode/
.idea/
__pycache__/
*.pyc
.next/
coverage/
.trae/
.update-temp/
apply-update.bat
# 数据库文件（�?B �?cookie、用户密码哈希等敏感数据，禁止提�?打包�?*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# 生产服务运行时状�?.prod.pids.json
.prod.ports.json

# 服务器文件上传目录（用户上传的视频等，不纳入版本控制�?# 旧路径：backend/uploads/
# 新路径：config/uploads/（自 v2 起所有数据统一存放�?config/ 目录下）
backend/uploads/

# 数据根目录：所有持久化数据（数据库、用户上传文件、头像、NMS 媒体切片等）
# 升级时仅需保留 config/ 目录即可保留全部数据；首次部署时由后端自动创�?config/

# 运行时日志目录（前后端实时日志与历史归档�?log/
