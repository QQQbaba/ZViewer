# 2026-07-23

## 重写视频播放控制栏
- 目标：重写代码、UI 风格保持一致、删去重复功能组件、实现自由缩放自动匹配宽度。范围覆盖主控制栏 + 屏幕共享三套控制栏。
- 新建共享模块 `src/components/VideoControls/`：`IconButton.tsx`（统一方形图标按钮）+ `controls.css`（用 CSS 容器查询单位 `cqw` 实现 `.vc-container` / `.vc-icon-btn` / `.vc-gap` 随容器宽度自由缩放）+ `index.ts`。这是「自由缩放自动匹配宽度」与「去重」的核心。
- 重写 `src/components/VideoPlayer/VideoControls.tsx`：拆出 `ProgressTrack.tsx`（进度条/拖拽/缓冲/同步线/tooltip）与 `SettingsPanel.tsx`（字幕/弹幕设置），主文件只做编排；`VideoControlsProps` / `VideoControlsHandle` 契约保持不变，`WatchTogetherPanel` 无需改动。
- 统一 `FlvPlayer.tsx`、`WatchControlsBar.tsx`、`ShareControlsBar.tsx`：改用 `IconButton` + `glass-strong` + `vc-container`，去掉 FlvPlayer 里手写的原始 `<button>`。
- 验证：`tsc --noEmit` 通过；`vite build` 2031 模块全部转换成功（仅 dist 的 safe-delete 因沙箱限制失败，改用 --outDir 临时目录验证通过）。
- 注意：`VideoControls` 的 `isWebFullscreen` / `onToggleWebFullscreen` props 在接口中保留但当前未使用（旧代码亦如此）。

## 视频加载/缓存/播放器全量重构（前后端）

完成房间内视频加载、视频缓存、播放器代码的完全重写（保持外部接口不变）：

**前端 player 模块（v2）**
- `engines/mse/stream-cache.ts`：IndexedDB 缓存 v2——覆盖式范围查询（缓存条目完全覆盖请求区间即命中并返回子切片）、LRU 淘汰（256MB 上限）、TTL 7 天；DB version 1→2 自动重建。
- `services/buffer-manager.ts`：SourceBuffer 串行操作队列（WeakMap），append/remove 全部入队消除 updating 竞争；QuotaExceededError 在队列内 prune+重试（注意：队列项内不能再 enqueue，需用 removeInQueue/appendInQueue，否则死锁）。
- `engines/mse/track.ts`（新增）：MediaTrack 单轨生命周期（loadHead/stream/seekStream），attach 与 seekTo 共用双轨编排。
- `engines/mse/player.ts`：MsePlayer 状态机（idle/attaching/attached/seeking/disposed）替代布尔 flag；首次 append 用 deferred gate + timeout race 替代 50ms 轮询；消除 video._mseAbortController hack。
- `hooks/usePlayerSource.ts`：Promise 串行队列替代 isAttaching/isReloading 双锁；forceReload 多次调用合并为最新 source。
- 4 个引擎（direct/hls/flv/mse）重写，metadata 等待失败时确保实例销毁。

**加载链路**
- `sync-playback/hooks/useVideoSource.ts`：结构清理（toPlayerSource/takeSnapshot/restoreSnapshot 提为模块级纯函数），行为不变。
- 新增 `room/watch-together/movie-source-resolver.ts`：影片→播放源解析决策（B站 在线解析 / recovery 复用旧 URL / 其他源直用），从 useWatchTogether.loadMovie 抽取，loadMovie 只留编排。reusedRecoveryUrl 标记驱动 attach 失败时的回退重新解析。

**后端流服务**
- `services/proxy/http-proxy.ts`：上游超时 30s（只覆盖连接+响应头阶段，fetch resolve 后取消——开放式 Range 下载 body 传输可能数分钟，不能超时中断）、客户端断连 AbortController 中断上游、超时 504、响应头白名单透传。
- `services/proxy/range-stream.ts`：新增 sendRangeNotSatisfiable（416 统一出口）、客户端断连销毁上游流。
- `routes/stream/resolve.ts`：NdjsonWriter 类封装 NDJSON 流式响应。

**验证**：前端 tsc + vite build 全通过（2033 模块）。后端 tsc 我改动的文件零错误；剩余 3 个错误是工作区预先存在的（用户已删除 animeSources.ts / sync-state.handler.ts / sync-control.handler.ts 但 index.ts 仍 import，与本次无关）。

**构建坑**：vite build 的 emptyOutDir 在沙箱 safe-delete 下对 dist 里带空格文件名会失败，需先手动 rm -rf frontend/dist。

## 视频播放器控制栏重写（v2）

重写 `frontend/src/components/VideoPlayer/` 控制栏，VideoControlsProps（33 个 props）/ VideoControlsHandle 契约 100% 不变，WatchTogetherPanel 零改动。

**新增结构**：
- `hooks/useControlsVisibility.ts`：自动隐藏状态机（idle 3s 隐藏 / 面板打开常显 / showControls 句柄）。注意：初始挂载不调度隐藏（保持旧版"初始常显直到首次鼠标活动"），仅在面板从打开→关闭时恢复计时。
- `hooks/usePanelDismiss.ts`：面板关闭三路（外点击/Escape/resize）。
- `hooks/usePlayerShortcuts.ts`：document 级键盘快捷键（Space 播放暂停 / M 静音 / F 全屏 / ←→ ±5s(Shift 10s) / ↑↓ 音量）。输入框聚焦（input/textarea/select/contenteditable）与 Ctrl/Meta/Alt 组合键时忽略；readOnly 观众端仅开放音量+全屏。
- `parts/DanmakuInput.tsx`、`parts/VolumeControl.tsx`、`parts/RateControl.tsx`（RateSelect 桌面/RateGrid 移动）、`parts/OverflowMenu.tsx`：消除桌面/移动端重复 UI（原弹幕输入/音量/倍速/同步/重载各写两遍）。

**主组件** VideoControls.tsx：668 行 → ~340 行，纯编排。
**ProgressTrack.tsx**：移除局部键盘处理（统一走 document 级快捷键，避免双重触发），保留 tabIndex/ARIA。

验证：tsc + vite build 全通过。
